const buildPoseidon = require("circomlibjs").buildPoseidon;

let poseidon;
/*
beforeEach(async function () {
    poseidon = await buildPoseidon();
});

describe('Build', () => {
    it('Create Tree', () => {
        let input = "Hello World!!!!!!!!!!!!!!!!!!!!!";
        let inputEncoded = new TextEncoder().encode(input);
        console.log(inputEncoded);
        let digest = poseidon([]);
        console.log(digest);
    });
});
*/
